version: 0.2
os: linux

# env:
#   parameter-store:
   

phases:
  install:
    commands:
      #- sudo apt-get update
      - ls -ltr
      - pwd
      - aws s3 cp --recursive "$CODEBUILD_SRC_DIR" s3://dbr-artifact
# artifacts:
#   type: zip
#   files:
#     - '~/dbr-aws-infra'