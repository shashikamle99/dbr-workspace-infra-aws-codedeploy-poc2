version: 0.2
os: linux

# env:
#   parameter-store:
   

phases:
  install:
    commands:
      #- sudo apt-get update
      - ls -ltr
      - pwd
artifacts:
  type: zip
  files:
    - './CODEBUILD_SRC_DIR'